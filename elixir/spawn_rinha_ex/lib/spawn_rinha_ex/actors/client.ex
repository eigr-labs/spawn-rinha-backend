defmodule SpawnRinhaEx.Actors.Client do
  @moduledoc """

  """
  alias SpawnRinhaEx.Actors.Account

  def credit(id, value, description) do
  end

  def debit(id, value, description) do
  end

  def statement(id) do
  end
end
